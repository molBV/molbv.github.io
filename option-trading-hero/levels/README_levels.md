# Levels

Drop Sonic recorder JSON files in this folder for storage.

For browser security reasons, `index.html` cannot automatically scan local folders when opened directly from disk. Use the **Load another Sonic JSON level** input on the start screen to load a recorder file.

V4 simulation mode generates a synthetic level from whichever JSON level is currently loaded.
